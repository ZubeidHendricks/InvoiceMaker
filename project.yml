# Generated by AppFactoryKit appgen. Run: xcodegen generate
name: InvoiceMaker
options:
  bundleIdPrefix: com.zubeid
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

packages:
  AppFactoryKit:
    url: https://github.com/ZubeidHendricks/AppFactoryKit
    from: "0.1.0"

targets:
  InvoiceMaker:
    type: application
    platform: iOS
    sources:
      - Sources
    dependencies:
      - package: AppFactoryKit
        product: AppFactoryKit
      - package: AppFactoryKit
        product: AppFactoryKitRevenueCat
    info:
      path: Sources/Info.plist
      properties:
        CFBundleDisplayName: "InvoiceMaker"
        UILaunchScreen: {}
        UISupportedInterfaceOrientations: [UIInterfaceOrientationPortrait]
    settings:
      base:
        PRODUCT_NAME: InvoiceMaker
        PRODUCT_BUNDLE_IDENTIFIER: com.zubeid.invoicemaker
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        TARGETED_DEVICE_FAMILY: "1"
        SWIFT_VERSION: "5.0"
        GENERATE_INFOPLIST_FILE: NO
        CODE_SIGN_STYLE: Automatic

  InvoiceMakerTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - Tests
      - path: Sources/Invoice.swift
    settings:
      base:
        PRODUCT_NAME: InvoiceMakerTests
        PRODUCT_BUNDLE_IDENTIFIER: com.zubeid.invoicemaker.tests
        GENERATE_INFOPLIST_FILE: YES
        TARGETED_DEVICE_FAMILY: "1"
        SWIFT_VERSION: "5.0"

schemes:
  Tests:
    build:
      targets:
        InvoiceMakerTests: [test]
    test:
      targets:
        - InvoiceMakerTests
